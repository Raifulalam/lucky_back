const mongoose = require('mongoose');
const { Schema } = mongoose;

const ComplaintsSchema = new Schema({
    name: {type: 'string',required: true},
    //add more properties and do not overwrite existing
    
});
